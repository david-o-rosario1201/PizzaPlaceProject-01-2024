@page "/"

<h1>Hello, world!</h1>

Welcome to your new app.

@code {
	private Estado Estado { get; } = new Estado();

	protected override async Task OnInitializedAsync()
	{
		await Task.Delay(500);

		Estado.Menu.Agregar(new Pizza(1, "Pepperoni", 8.99M, Picantez.Picante));
		Estado.Menu.Agregar(new Pizza(2, "Margarita", 7.99M, Picantez.Ninguno));
		Estado.Menu.Agregar(new Pizza(3, "Diabolo", 9.99M, Picantez.Extremo));
	}
}